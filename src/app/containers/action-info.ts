/* This class is used for taking widget operation information to widgets. */
export class ActionInfo {
    type: any;
    action: string = null;
    currentlySelectedIndex: number = 0;
    totalWidgetCount: number = 0;
    // subAction: string = null;
    // widgetId = -1;
    // panelNumber = -1;
    // widgetType = 1;
    // /* Action inputs specific to lower panel operation.*/
    // isAll = false;
    // graphVisibility = true;
    // rowIndex: number = -1;
    // index = 0;
    // data: any;
    // graphName: string = null;
    // isZoomed = false;
    // zoomStartTime = '';
    // zoomEndTime = '';
    // isZoomAddInList = false;
    // isSinglePanelView = false;
}