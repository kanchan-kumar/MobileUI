import { WidgetInfo } from './widget-info';

/**
 * This interface contains all the widgets information of layout.
 */
export interface PanelLayoutInfo {
  layoutId: number;
  layoutName: number;
  widgets: WidgetInfo[];
}